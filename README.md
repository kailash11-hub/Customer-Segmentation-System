# Customer-Segmentation-System
Built an end-to-end Customer Segmentation System using unsupervised clustering (K-Means, DBSCAN) on demographic and behavioral data, validated with silhouette scoring; trained ensemble classifiers (Random Forest, XGBoost) to assign new customers to segments in real time, achieving 91–97% cross-validated accuracy against a majority-class baseline.
